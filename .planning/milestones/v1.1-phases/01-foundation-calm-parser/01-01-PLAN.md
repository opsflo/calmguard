---
phase: 01-foundation-calm-parser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - components.json
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/lib/utils.ts
autonomous: true

must_haves:
  truths:
    - "Developer can run pnpm dev and see Next.js app running on localhost"
    - "App renders with dark theme (slate-900 background) by default"
    - "TypeScript strict mode is enabled with no any types"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next"
    - path: "tsconfig.json"
      provides: "TypeScript strict configuration"
      contains: "strict"
    - path: "src/app/layout.tsx"
      provides: "Root layout with ThemeProvider and dark theme"
      contains: "ThemeProvider"
    - path: "src/app/globals.css"
      provides: "Tailwind CSS with dark theme defaults"
      contains: "tailwindcss"
    - path: "components.json"
      provides: "shadcn/ui configuration"
      contains: "shadcn"
  key_links:
    - from: "src/app/layout.tsx"
      to: "next-themes"
      via: "ThemeProvider wrapping children"
      pattern: "ThemeProvider.*defaultTheme.*dark"
    - from: "src/app/globals.css"
      to: "tailwind.config.ts"
      via: "Tailwind CSS imports"
      pattern: "@tailwind|@import.*tailwindcss"
---

<objective>
Initialize the Next.js 14+ project with TypeScript strict mode, pnpm, shadcn/ui with dark theme, and all foundational configuration.

Purpose: Establish the build system, styling, and component library that all subsequent plans depend on.
Output: Bootable Next.js app with dark theme, shadcn/ui components, and TypeScript strict mode.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-calm-parser/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with pnpm and core dependencies</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    pnpm-lock.yaml
  </files>
  <action>
    Run `pnpm create next-app@latest . --typescript --tailwind --app --turbopack --src-dir --import-alias "@/*" --yes` from the project root (calmguard/).

    IMPORTANT: The project root already has .planning/ and CLAUDE.md files. The create-next-app command should scaffold into existing directory without overwriting planning files.

    After scaffolding, verify and harden tsconfig.json:
    - Ensure `"strict": true` is set
    - Add `"noUnusedLocals": true`
    - Add `"noUnusedParameters": true`
    - Add `"noImplicitReturns": true`

    Install additional core dependencies:
    ```bash
    pnpm add zod zustand next-themes
    ```

    Initialize shadcn/ui:
    ```bash
    pnpm dlx shadcn@latest init --defaults
    ```

    Add required shadcn/ui components:
    ```bash
    pnpm dlx shadcn@latest add button card skeleton dropdown-menu select separator badge scroll-area
    ```

    Verify the project builds:
    ```bash
    pnpm build
    ```
  </action>
  <verify>
    - `pnpm dev` starts the dev server without errors
    - `pnpm build` completes without TypeScript errors
    - `tsconfig.json` has `"strict": true`
    - `package.json` contains `next`, `zod`, `zustand`, `next-themes` dependencies
    - `components.json` exists (shadcn/ui initialized)
    - `src/components/ui/button.tsx` exists (shadcn components installed)
  </verify>
  <done>Next.js project boots with TypeScript strict mode, all dependencies installed, shadcn/ui initialized with required components.</done>
</task>

<task type="auto">
  <name>Task 2: Configure dark theme with next-themes and Tailwind</name>
  <files>
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
    tailwind.config.ts
  </files>
  <action>
    Configure the root layout (`src/app/layout.tsx`):
    - Wrap children with `ThemeProvider` from `next-themes`
    - Set `attribute="class"`, `defaultTheme="dark"`, `enableSystem`, `disableTransitionOnChange`
    - Add `suppressHydrationWarning` to the `<html>` tag (required for next-themes hydration)
    - Set body className to `"min-h-screen bg-background font-sans antialiased"` (shadcn/ui convention)

    Update `globals.css`:
    - Ensure shadcn/ui CSS variables are present for dark theme
    - Override dark theme CSS variables to use slate palette:
      - `--background: 222.2 84% 4.9%` (slate-950)
      - `--foreground: 210 40% 98%` (slate-50)
      - `--card: 217.2 32.6% 11%` (slate-900)
      - `--card-foreground: 210 40% 98%`
    - Keep the rest of shadcn/ui default dark variables

    Update the default `page.tsx` to show a simple dark-themed placeholder:
    - Display "CALMGuard" as h1 with slate-50 text
    - Subtext: "CALM-native continuous compliance platform"
    - Centered on screen with slate-900 background
    - This is temporary — will be replaced in Plan 04

    Verify dark mode renders correctly:
    ```bash
    pnpm dev
    ```
  </action>
  <verify>
    - `pnpm dev` shows dark-themed page with "CALMGuard" text
    - No hydration mismatch warnings in browser console
    - `src/app/layout.tsx` contains `ThemeProvider` with `defaultTheme="dark"`
    - `src/app/layout.tsx` has `suppressHydrationWarning` on html tag
    - Background is dark (slate-900/950), text is light (slate-50)
  </verify>
  <done>App renders with dark theme by default using next-themes, slate palette applied, no hydration warnings.</done>
</task>

</tasks>

<verification>
1. `pnpm dev` starts without errors
2. `pnpm build` completes without errors
3. Browser shows dark-themed "CALMGuard" page
4. No console warnings or hydration mismatches
5. `tsconfig.json` confirms strict mode enabled
6. shadcn/ui components render correctly (button, card, skeleton visible in dev tools)
</verification>

<success_criteria>
- Next.js 14+ project running with App Router
- TypeScript strict mode with no any types
- shadcn/ui initialized with dark theme (slate palette)
- pnpm as package manager with zod, zustand, next-themes installed
- Clean build with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-calm-parser/01-01-SUMMARY.md`
</output>
