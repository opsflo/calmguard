---
phase: 01-foundation-calm-parser
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - src/app/page.tsx
  - src/app/dashboard/page.tsx
  - src/app/dashboard/layout.tsx
  - src/components/calm/architecture-selector.tsx
  - src/components/calm/parse-error-display.tsx
  - src/components/dashboard/sidebar.tsx
  - src/components/dashboard/header.tsx
  - src/components/dashboard/compliance-card-skeleton.tsx
  - src/components/dashboard/architecture-graph-skeleton.tsx
  - src/components/dashboard/agent-feed-skeleton.tsx
  - src/components/layout/dashboard-layout.tsx
autonomous: true

must_haves:
  truths:
    - "User can select a demo CALM architecture from a dropdown on the landing page"
    - "Selected architecture parses and stores in Zustand with status shown"
    - "Invalid CALM JSON displays clear error messages listing each validation issue"
    - "Dashboard skeleton shows loading placeholders for compliance gauge, architecture graph, agent feed, and pipeline preview"
    - "Sidebar shows navigation items (Overview, Architecture, Compliance, Pipeline, Findings)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Landing page with architecture selector and upload area"
    - path: "src/app/dashboard/page.tsx"
      provides: "Dashboard overview page with skeleton loaders"
    - path: "src/components/calm/architecture-selector.tsx"
      provides: "Dropdown to select demo architectures"
      exports: ["ArchitectureSelector"]
    - path: "src/components/calm/parse-error-display.tsx"
      provides: "Error display component for CALM validation failures"
      exports: ["ParseErrorDisplay"]
    - path: "src/components/dashboard/sidebar.tsx"
      provides: "Left sidebar with navigation items and agent status indicators"
      exports: ["Sidebar"]
  key_links:
    - from: "src/components/calm/architecture-selector.tsx"
      to: "src/lib/calm/parser.ts"
      via: "parseCalm called on demo selection"
      pattern: "parseCalm|parseCalmFromString"
    - from: "src/components/calm/architecture-selector.tsx"
      to: "src/store/analysis-store.ts"
      via: "setCalmData stores parsed result"
      pattern: "useAnalysisStore|setCalmData"
    - from: "src/components/calm/architecture-selector.tsx"
      to: "examples/index.ts"
      via: "imports DEMO_ARCHITECTURES for dropdown options"
      pattern: "DEMO_ARCHITECTURES"
    - from: "src/app/page.tsx"
      to: "src/app/dashboard/page.tsx"
      via: "router.push after successful parse"
      pattern: "router\\.push.*dashboard|redirect.*dashboard"
---

<objective>
Build the landing page with architecture selector and the dashboard skeleton with navigation sidebar and loading placeholders.

Purpose: Complete the user-facing UI for Phase 1 — user selects a demo, it parses, and they see a dashboard shell with skeleton loading states ready for Phase 2+ content.
Output: Landing page, dashboard layout with sidebar, skeleton loading components for all dashboard panels.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-calm-parser/01-RESEARCH.md
@.planning/phases/01-foundation-calm-parser/01-01-SUMMARY.md
@.planning/phases/01-foundation-calm-parser/01-02-SUMMARY.md
@.planning/phases/01-foundation-calm-parser/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build landing page with architecture selector and error display</name>
  <files>
    src/app/page.tsx
    src/components/calm/architecture-selector.tsx
    src/components/calm/parse-error-display.tsx
  </files>
  <action>
    **Architecture Selector (`src/components/calm/architecture-selector.tsx`):**

    Mark as `'use client'` (needs hooks, events, Zustand).

    Build a component that:
    1. Imports `DEMO_ARCHITECTURES` from `@/examples`
    2. Renders a shadcn/ui `Select` dropdown with demo options showing name + node count
    3. On selection:
       - Sets loading state in store via `setStatus('loading')`
       - Calls `parseCalm(selectedDemo.data)` from `@/lib/calm/parser`
       - On success: calls `extractAnalysisInput()`, stores via `setCalmData()`
       - On failure: displays error via `setError()`
    4. Shows a "Start Analysis" button (disabled until architecture is parsed) that navigates to `/dashboard`
    5. Below the selector, show a file upload drop zone (visual placeholder for Phase 6):
       - Dashed border area with "Drop CALM JSON here" text
       - Non-functional for now — just visual. Add a subtle "Coming soon" badge

    **Parse Error Display (`src/components/calm/parse-error-display.tsx`):**

    Mark as `'use client'`.

    Build a component that renders parse errors:
    - Red border card (border-red-500/50, bg-red-950/20)
    - "Validation Error" header with X icon
    - Error message
    - List of issues with: path (monospace font), message
    - "Dismiss" button to clear error

    Props: `{ error: { message: string; issues: Array<{ path: string; message: string; code: string }> } | null; onDismiss: () => void }`

    **Landing Page (`src/app/page.tsx`):**

    Replace the placeholder from Plan 01. Build a centered landing page:
    - "CALMGuard" logo/title (text-3xl font-bold text-slate-50)
    - Subtitle: "CALM-native continuous compliance platform" (text-slate-400)
    - Brief tagline: "Upload a FINOS CALM architecture and get instant AI-powered compliance analysis"
    - `<ArchitectureSelector />` component
    - `<ParseErrorDisplay />` component (shown conditionally from Zustand error state)
    - Dark background (bg-slate-950), centered content with max-w-2xl
    - Use shadcn/ui Card for the selector area

    **Navigation:**
    Use `next/navigation` `useRouter` in the ArchitectureSelector to navigate to `/dashboard` when "Start Analysis" is clicked (after successful parse).
  </action>
  <verify>
    - Landing page renders with "CALMGuard" title and architecture selector
    - Dropdown shows 2 demo options (Trading Platform, Payment Gateway)
    - Selecting a demo triggers parsing and shows success state
    - Selecting invalid data shows ParseErrorDisplay with issues
    - "Start Analysis" button navigates to /dashboard
    - All components use shadcn/ui primitives (Card, Select, Button, Badge)
    - No `any` types
  </verify>
  <done>Landing page with architecture selector dropdown, parse error display, and navigation to dashboard. User can select demo architecture and proceed to dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: Build dashboard layout with sidebar and skeleton loaders</name>
  <files>
    src/app/dashboard/layout.tsx
    src/app/dashboard/page.tsx
    src/components/dashboard/sidebar.tsx
    src/components/dashboard/header.tsx
    src/components/dashboard/compliance-card-skeleton.tsx
    src/components/dashboard/architecture-graph-skeleton.tsx
    src/components/dashboard/agent-feed-skeleton.tsx
    src/components/layout/dashboard-layout.tsx
  </files>
  <action>
    **Dashboard Layout (`src/app/dashboard/layout.tsx`):**

    Server Component layout that wraps dashboard pages:
    ```tsx
    export default function DashboardLayout({ children }) {
      return <DashboardShell>{children}</DashboardShell>;
    }
    ```

    **Dashboard Shell (`src/components/layout/dashboard-layout.tsx`):**

    Mark as `'use client'` (needs Zustand for sidebar state).

    Implement a two-column layout:
    - Left sidebar (w-64, bg-slate-900, border-r border-slate-800)
    - Main content area (flex-1, bg-slate-950, overflow-auto)
    - Top header bar (h-14, border-b border-slate-800)

    **Sidebar (`src/components/dashboard/sidebar.tsx`):**

    Mark as `'use client'`.

    Navigation items (each with icon placeholder and label):
    1. "Overview" (LayoutDashboard icon) — active by default
    2. "Architecture" (Network icon)
    3. "Compliance" (Shield icon)
    4. "Pipeline" (GitBranch icon)
    5. "Findings" (AlertTriangle icon)

    Below navigation, show "Agent Status" section:
    - List of 4 agents: Architecture Analyzer, Compliance Mapper, Pipeline Generator, Risk Scorer
    - Each with a status dot: gray (idle) — all idle in Phase 1
    - This section is visual scaffolding — dots will light up in Phase 2+

    Use `lucide-react` icons (installed with shadcn/ui).

    Style: active item has `bg-slate-800 text-slate-50`, inactive has `text-slate-400 hover:text-slate-200 hover:bg-slate-800/50`.

    **Header (`src/components/dashboard/header.tsx`):**

    Mark as `'use client'`.

    - Left: "CALMGuard" text + currently selected architecture name from Zustand store
    - Right: Badge showing parse status (e.g., "Parsed: 10 nodes, 8 relationships")
    - Use slate-800 background, border-b

    **Dashboard Overview Page (`src/app/dashboard/page.tsx`):**

    Mark as `'use client'` (accesses Zustand).

    If no CALM data in store: show "No architecture loaded" message with link back to landing page.

    If CALM data exists: show grid layout with skeleton loaders:
    - 2x2 grid on desktop (grid-cols-2), stacked on mobile
    - Top-left: Compliance Score skeleton (circular gauge placeholder)
    - Top-right: Architecture Graph skeleton (large rectangle with node placeholders)
    - Bottom-left: Agent Activity Feed skeleton (list of message bars)
    - Bottom-right: Pipeline Preview skeleton (code block lines)

    **Skeleton Components:**

    Create 3 skeleton components using shadcn/ui `<Skeleton>`:

    1. `compliance-card-skeleton.tsx`: Card with circular skeleton (w-32 h-32 rounded-full) + horizontal bar skeletons below
    2. `architecture-graph-skeleton.tsx`: Large card with scattered rectangle skeletons (simulating nodes) and line skeletons (simulating edges)
    3. `agent-feed-skeleton.tsx`: Card with 5 horizontal bar skeletons of varying widths (simulating feed messages)

    **IMPORTANT styling constraints:**
    - All cards: `bg-slate-900 border-slate-800 rounded-lg`
    - Skeleton shimmer: use shadcn/ui default (handles animation)
    - Grid gap: `gap-4`
    - Padding: `p-6` on the main content area
    - Each skeleton card should have a title (e.g., "Compliance Score", "Architecture Graph") in slate-400 text
  </action>
  <verify>
    - `/dashboard` renders with sidebar + header + content area
    - Sidebar shows 5 navigation items with icons
    - Sidebar shows 4 agent status indicators (all gray/idle)
    - Header shows "CALMGuard" and loaded architecture info
    - Content area shows 4 skeleton cards in a grid
    - Each skeleton card has a title and animated placeholder content
    - Redirects to landing if no CALM data loaded
    - Dark theme consistent (slate-900/950 palette)
    - `pnpm build` passes
  </verify>
  <done>Dashboard layout with left sidebar (5 nav items + 4 agent status dots), header with architecture info, and 4 skeleton loading cards (compliance, architecture, agent feed, pipeline). All dark-themed with slate palette.</done>
</task>

</tasks>

<verification>
1. Full user flow works: Landing page -> Select demo -> Parse -> Navigate to Dashboard
2. Dashboard shows sidebar with navigation and agent status indicators
3. All 4 skeleton cards render with proper animations
4. Invalid CALM JSON shows error display with issue details
5. `pnpm build` passes with zero errors
6. Dark theme consistent across all pages (slate-900/950)
7. No hydration warnings in browser console
</verification>

<success_criteria>
- User can select demo architecture from dropdown (CALM-01)
- CALM JSON parses into typed AnalysisInput (CALM-02)
- Validation errors display clearly (CALM-03)
- Dashboard shows skeleton loaders for all component areas (INFRA-03)
- Sidebar has navigation items and agent status section
- All Phase 1 requirements (CALM-01, CALM-02, CALM-03, INFRA-03) are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-calm-parser/01-04-SUMMARY.md`
</output>
