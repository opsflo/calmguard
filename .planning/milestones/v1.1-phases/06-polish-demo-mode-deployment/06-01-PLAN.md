---
phase: 06-polish-demo-mode-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/calm/cli-validator.ts
  - src/app/api/calm/validate/route.ts
  - src/components/calm/calm-upload-zone.tsx
  - src/components/calm/architecture-selector.tsx
  - src/components/dashboard/sidebar.tsx
  - src/store/analysis-store.ts
  - src/app/api/analyze/route.ts
  - package.json
autonomous: true
requirements: [CALM-04, CALM-05, INFRA-01]

must_haves:
  truths:
    - "User can drag-and-drop a .json file onto the sidebar upload zone and see parsing/validating/ready status progression"
    - "User can click the upload zone to browse and select a file via native file picker"
    - "Invalid JSON or non-CALM files show inline error messages in the upload zone"
    - "Valid uploaded CALM JSON gets parsed, sets store state to 'parsed', and is ready for analysis"
    - "Vercel SSE streaming works with 300s max duration for production deployment"
  artifacts:
    - path: "src/lib/calm/cli-validator.ts"
      provides: "calm-cli subprocess wrapper for CALM schema validation"
      exports: ["validateWithCalmCli", "CalmValidationResult"]
    - path: "src/app/api/calm/validate/route.ts"
      provides: "POST /api/calm/validate endpoint (Node.js runtime)"
      exports: ["POST"]
    - path: "src/components/calm/calm-upload-zone.tsx"
      provides: "Drag-and-drop file upload with inline status and validation"
      exports: ["CalmUploadZone"]
  key_links:
    - from: "src/components/calm/calm-upload-zone.tsx"
      to: "/api/calm/validate"
      via: "fetch POST after local Zod parse"
      pattern: "fetch.*api/calm/validate"
    - from: "src/components/calm/calm-upload-zone.tsx"
      to: "src/store/analysis-store.ts"
      via: "setCalmData on successful validation"
      pattern: "setCalmData"
    - from: "src/app/api/calm/validate/route.ts"
      to: "src/lib/calm/cli-validator.ts"
      via: "validateWithCalmCli import"
      pattern: "validateWithCalmCli"
---

<objective>
Implement custom CALM JSON file upload with drag-and-drop, calm-cli validation, and Vercel deployment configuration.

Purpose: Users must be able to upload their own CALM architecture files (not just demo presets) for the hackathon demo. This is the primary interactive feature that differentiates CALMGuard from a static demo. Vercel maxDuration ensures SSE streaming works in production.

Output: Working drag-and-drop upload zone in sidebar, /api/calm/validate route with calm-cli subprocess, maxDuration export on analyze route.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-demo-mode-deployment/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/store/analysis-store.ts:
```typescript
interface AnalysisState {
  selectedDemoId: string | null;
  rawCalmData: CalmDocument | null;
  analysisInput: AnalysisInput | null;
  analysisResult: AnalysisResult | null;
  agentEvents: AgentEvent[];
  activeAgents: string[];
  selectedFrameworks: string[];
  status: AnalysisStatus; // 'idle' | 'loading' | 'parsed' | 'analyzing' | 'complete' | 'error'
  error: ParseError['error'] | null;
  // Actions:
  setCalmData: (data: CalmDocument, input: AnalysisInput) => void;
  setStatus: (status: AnalysisStatus) => void;
  setError: (error: ParseError['error']) => void;
  reset: () => void;
}
```

From src/lib/calm/parser.ts:
```typescript
export function parseCalm(data: unknown): ParseSuccess | ParseError;
```

From src/lib/calm/extractor.ts:
```typescript
export function extractAnalysisInput(doc: CalmDocument): AnalysisInput;
```

From src/components/calm/architecture-selector.tsx:
```typescript
// Lines 144-159: File Upload Placeholder with "Coming soon" badge
// This entire block must be REPLACED with real CalmUploadZone component
```

From src/app/api/analyze/route.ts:
```typescript
export const dynamic = 'force-dynamic';
// MISSING: export const maxDuration = 300; — needs to be added for INFRA-01
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build calm-cli validator, /api/calm/validate route, and Vercel maxDuration config</name>
  <files>
    src/lib/calm/cli-validator.ts
    src/app/api/calm/validate/route.ts
    src/app/api/analyze/route.ts
    package.json
  </files>
  <action>
1. Install @finos/calm-cli as a dependency:
   ```
   pnpm add @finos/calm-cli
   ```

2. Create `src/lib/calm/cli-validator.ts`:
   - Export `CalmValidationResult` interface: `{ valid: boolean; errors: Array<{ message: string; path?: string }> }`
   - Export `validateWithCalmCli(calmJson: unknown): Promise<CalmValidationResult>`
   - Implementation: write JSON to temp file (`os.tmpdir()` + `calm-validate-${Date.now()}.json`), spawn calm-cli as subprocess
   - Use `execFile('node', [require.resolve('@finos/calm-cli/dist/index.js'), 'validate', '-a', tmpFile, '--format', 'json'])` — safe path resolution that works on both local dev and Vercel (no PATH lookup)
   - Set `timeout: 15_000` on execFileAsync options
   - Parse stdout as JSON array of validation errors
   - Handle non-zero exit code (validation errors in stdout), JSON parse failure (CLI crash), and timeout
   - ALWAYS clean up temp file in `finally` block via `unlink(tmpFile).catch(() => {})`
   - Use `promisify(execFile)` from Node.js `child_process` and `util`

3. Create `src/app/api/calm/validate/route.ts`:
   - `export const runtime = 'nodejs'` — REQUIRED, child_process not available in Edge Runtime
   - Export async `POST(req: NextRequest)` handler
   - Parse request body as `{ calm: unknown }`
   - Call `validateWithCalmCli(body.calm)`
   - Return `Response.json(result)` with the CalmValidationResult
   - On unexpected errors, return 500 with `{ valid: false, errors: [{ message: 'Validation service error' }] }`

4. Add `export const maxDuration = 300;` to `src/app/api/analyze/route.ts` — add immediately after the existing `export const dynamic = 'force-dynamic';` line. This enables Vercel Fluid Compute's 300-second timeout for SSE streaming in production.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/opsflow-sh/hackathon/dtcch-2026-opsflow-llc && pnpm typecheck</automated>
  </verify>
  <done>
    - cli-validator.ts exports validateWithCalmCli with correct subprocess spawning and temp file cleanup
    - /api/calm/validate route exists with runtime='nodejs' and POST handler
    - analyze route has maxDuration=300 export
    - @finos/calm-cli is in package.json dependencies
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build CalmUploadZone component and wire into sidebar + architecture-selector</name>
  <files>
    src/components/calm/calm-upload-zone.tsx
    src/components/calm/architecture-selector.tsx
    src/components/dashboard/sidebar.tsx
    src/store/analysis-store.ts
  </files>
  <action>
1. Create `src/components/calm/calm-upload-zone.tsx` — a 'use client' component:
   - Import parseCalm, extractAnalysisInput, useAnalysisStore
   - Define local type: `UploadStatus = 'idle' | 'parsing' | 'validating' | 'ready' | 'error'`
   - State: `status: UploadStatus`, `errors: string[]`, `isDragOver: boolean`, `fileName: string | null`
   - Hidden `<input type="file" accept=".json" ref={inputRef} />` for click-to-browse
   - Drop zone `<div>` with `onDrop`, `onDragOver` (preventDefault + setIsDragOver(true)), `onDragLeave` (setIsDragOver(false))
   - Click handler on the drop zone triggers `inputRef.current?.click()`
   - `processFile(file: File)` callback:
     a. Validate file extension is `.json` — if not, set error "Only .json files are accepted"
     b. Set status='parsing', read file text via `file.text()`
     c. `JSON.parse(text)` — catch and show "Invalid JSON" error
     d. `parseCalm(parsed)` — if !success, show "CALM schema validation failed"
     e. Set status='validating', call `fetch('/api/calm/validate', { method: 'POST', body: JSON.stringify({ calm: parsed }) })`
     f. Parse response — if !valid, show validation.errors mapped to strings
     g. If valid: `extractAnalysisInput(localResult.data)` → `setCalmData(localResult.data, input)` → set status='ready'
     h. Error recovery: show errors inline and allow re-upload (user decision: show errors + allow re-upload)
   - UI styling per dark theme:
     - Idle: dashed border (slate-700), Upload icon, "Drop CALM JSON here" text, "or click to browse" subtitle
     - Drag over: border-emerald-500, bg-emerald-500/5
     - Parsing: Loader2 spin icon, "Parsing JSON..." text
     - Validating: Loader2 spin icon, "Validating against CALM schema..." text
     - Ready: CheckCircle icon (emerald-500), file name displayed, "Ready for analysis" text
     - Error: XCircle icon (red-500), error messages listed, "Try another file" prompt
   - Status indicator shows progression: parsing → validating → ready (per user decision)

2. Update `src/components/calm/architecture-selector.tsx`:
   - REMOVE the entire "File Upload Placeholder" section (lines 143-160, the dashed div with "Coming soon" badge)
   - Import and render `<CalmUploadZone />` in its place
   - The upload zone visually replaces the placeholder — same position in the layout

3. Add CalmUploadZone to sidebar in `src/components/dashboard/sidebar.tsx`:
   - Import CalmUploadZone
   - Add a section below the navigation links and above the agent status section
   - Wrap in a `<div className="px-3 py-2">` with a small "Upload" label
   - This makes the upload zone always accessible while viewing the dashboard (per user decision: "always accessible while viewing the dashboard")

4. No store changes needed — existing `setCalmData` and `setStatus` actions are sufficient for the upload flow. The upload zone calls the same store actions as the demo selector.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/opsflow-sh/hackathon/dtcch-2026-opsflow-llc && pnpm typecheck && pnpm build</automated>
  </verify>
  <done>
    - CalmUploadZone component renders drag-and-drop zone with inline status progression
    - architecture-selector.tsx has CalmUploadZone replacing the "Coming soon" placeholder
    - Sidebar has CalmUploadZone section for dashboard-level file upload
    - Uploaded valid CALM file sets store to 'parsed' state, ready for analysis
    - Invalid files show inline error messages
    - Build succeeds without errors
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes — all new files compile under strict mode
2. `pnpm build` succeeds — Next.js production build includes new API route and components
3. `pnpm test:run` passes — existing tests not broken by store or route changes
4. /api/calm/validate route file exists with `runtime = 'nodejs'` export
5. /api/analyze route file has `maxDuration = 300` export
6. CalmUploadZone component exists and is imported in both architecture-selector and sidebar
</verification>

<success_criteria>
- User can drag-and-drop a .json file onto the upload zone (both on landing page and in sidebar)
- Upload zone shows parsing → validating → ready status progression inline
- Invalid files display specific error messages and allow re-upload
- Valid CALM JSON populates the Zustand store (same behavior as demo selector)
- analyze route has maxDuration=300 for Vercel deployment
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-demo-mode-deployment/06-01-SUMMARY.md`
</output>
