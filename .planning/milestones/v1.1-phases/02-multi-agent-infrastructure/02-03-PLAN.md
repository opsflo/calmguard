---
phase: 02-multi-agent-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/lib/agents/registry.ts
  - src/lib/skills/loader.ts
autonomous: true

must_haves:
  truths:
    - "loadAgentConfig() reads a YAML file from agents/ directory and returns typed AgentConfig"
    - "loadSkills() reads SKILL.md files from skills/ directory and returns concatenated markdown content"
    - "Registry validates YAML against AgentConfig schema and throws descriptive errors for invalid configs"
  artifacts:
    - path: "src/lib/agents/registry.ts"
      provides: "YAML agent config loader with validation"
      exports: ["loadAgentConfig", "loadAllAgentConfigs"]
    - path: "src/lib/skills/loader.ts"
      provides: "SKILL.md file reader that concatenates markdown content"
      exports: ["loadSkills", "loadSkill"]
  key_links:
    - from: "src/lib/agents/registry.ts"
      to: "agents/*.yaml"
      via: "reads YAML files from agents/ directory"
      pattern: "readFileSync.*agents.*yaml"
    - from: "src/lib/agents/registry.ts"
      to: "src/lib/agents/types.ts"
      via: "validates against AgentConfig schema"
      pattern: "agentConfigSchema"
    - from: "src/lib/skills/loader.ts"
      to: "skills/*.md"
      via: "reads markdown files from skills/ directory"
      pattern: "readFileSync.*skills.*md"
---

<objective>
Build the agent registry (YAML config loader) and skill loader (SKILL.md reader) that connect agent configurations to their runtime implementations.

Purpose: Agents need their YAML configs parsed into typed objects and their skill files loaded as prompt context. This bridges the content files (Plan 02) to the type system (Plan 01).
Output: Two loader modules — registry for YAML configs, loader for SKILL.md files.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multi-agent-infrastructure/02-01-SUMMARY.md
@.planning/phases/02-multi-agent-infrastructure/02-02-SUMMARY.md
@src/lib/agents/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent registry (YAML config loader)</name>
  <files>
    src/lib/agents/registry.ts
  </files>
  <action>
Create `src/lib/agents/registry.ts`:

1. Import `readFileSync`, `existsSync` from `fs`, `join` from `path`, `parse as parseYaml` from `yaml`.
2. Import `AgentConfig`, `agentConfigSchema` from `./types`.

3. `loadAgentConfig(name: string): AgentConfig`:
   - Build path: `join(process.cwd(), 'agents', name + '.yaml')`
   - Check `existsSync` — throw `Error('Agent config not found: ${path}')` if missing
   - Read file, parse YAML with `parseYaml()`
   - Validate against `agentConfigSchema` using `.safeParse()` — throw with formatted error if invalid
   - Return typed `AgentConfig`

4. `loadAllAgentConfigs(): Map<string, AgentConfig>`:
   - Read all `.yaml` files from `agents/` directory using `readdirSync`
   - Filter to `.yaml` extension
   - Load each with `loadAgentConfig`
   - Return `Map` keyed by `metadata.name`

5. Cache loaded configs in a module-level `Map` to avoid re-reading files on every call. Export `clearConfigCache()` for testing.

6. Error handling: Wrap YAML parse in try/catch, throw `Error('Failed to parse ${path}: ${error.message}')`.
  </action>
  <verify>Run `pnpm typecheck`. Verify registry can import types from `./types`. Verify `yaml` package is in dependencies (installed in Plan 01).</verify>
  <done>Registry loads YAML configs with validation, caching, and descriptive error messages. loadAgentConfig returns typed AgentConfig. loadAllAgentConfigs returns all agents as Map.</done>
</task>

<task type="auto">
  <name>Task 2: Create SKILL.md loader</name>
  <files>
    src/lib/skills/loader.ts
  </files>
  <action>
Create `src/lib/skills/loader.ts`:

1. Import `readFileSync`, `existsSync` from `fs`, `join` from `path`.

2. `loadSkill(skillPath: string): string`:
   - Resolve path relative to `process.cwd()`: `join(process.cwd(), skillPath)`
   - Check `existsSync` — throw `Error('Skill file not found: ${resolvedPath}')` if missing
   - Read file content with `readFileSync(resolvedPath, 'utf-8')`
   - Return content as string

3. `loadSkills(skillPaths: string[]): string`:
   - Load each skill via `loadSkill()`
   - Concatenate with `\n\n---\n\n` separator between files
   - Return concatenated string

4. `loadSkillsForAgent(config: AgentConfig): string`:
   - Convenience function: reads `config.spec.skills` array
   - Returns empty string if no skills defined
   - Calls `loadSkills(config.spec.skills)`

5. Cache loaded skills in a module-level `Map<string, string>` keyed by file path. Export `clearSkillCache()` for testing.

6. No `any` types. Use proper string return types.
  </action>
  <verify>Run `pnpm typecheck`. Verify skills loader can import AgentConfig from agents/types.</verify>
  <done>Skill loader reads SKILL.md files, concatenates with separators, and caches results. loadSkillsForAgent() convenience function reads skill paths from AgentConfig. Descriptive errors for missing files.</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes with zero errors
2. `pnpm build` succeeds
3. `src/lib/agents/registry.ts` exports loadAgentConfig, loadAllAgentConfigs, clearConfigCache
4. `src/lib/skills/loader.ts` exports loadSkill, loadSkills, loadSkillsForAgent, clearSkillCache
</verification>

<success_criteria>
- Registry loads and validates YAML agent configs with caching
- Skill loader reads and concatenates SKILL.md files with caching
- Both throw descriptive errors for missing/invalid files
- All exports are properly typed with no `any`
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-agent-infrastructure/02-03-SUMMARY.md`
</output>
