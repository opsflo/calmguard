---
phase: 03-api-routes-dashboard-core
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/page.tsx
  - src/components/graph/architecture-graph.tsx
  - src/components/graph/utils/layout.ts
  - src/components/layout/dashboard-layout.tsx
  - src/components/dashboard/agent-feed.tsx
autonomous: false
gap_closure: true
requirements: [DASH-01, VIZ-01, VIZ-02, FEED-01]

must_haves:
  truths:
    - "Architecture graph in overview card is large enough to see nodes and edges without scrolling"
    - "Architecture tab shows nodes well-distributed across the viewport, not clustered in center"
    - "Agent Activity panel extends full page height in the right column"
  artifacts:
    - path: "src/app/dashboard/page.tsx"
      provides: "Overview card with increased graph height"
      contains: "h-[500px]"
    - path: "src/components/graph/architecture-graph.tsx"
      provides: "Compact mode prop to hide MiniMap/Controls in overview, maxZoom cap"
      contains: "compact"
    - path: "src/components/graph/utils/layout.ts"
      provides: "Wider dagre spacing for cleaner node distribution"
      contains: "ranksep"
    - path: "src/components/layout/dashboard-layout.tsx"
      provides: "Full-height right column for agent feed"
      contains: "h-full"
    - path: "src/components/dashboard/agent-feed.tsx"
      provides: "Full-height Card and flex-based ScrollArea"
      contains: "h-full"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "src/components/graph/architecture-graph.tsx"
      via: "compact prop"
      pattern: "compact"
    - from: "src/components/layout/dashboard-layout.tsx"
      to: "src/components/dashboard/agent-feed.tsx"
      via: "h-full CSS chain"
      pattern: "h-full"
---

<objective>
Fix 3 UAT-diagnosed visual gaps in the Phase 3 dashboard: cramped overview graph, clustered architecture tab graph, and half-height agent activity panel.

Purpose: Close cosmetic/minor UAT issues so the dashboard looks polished before Phase 4 compliance visualizations.
Output: 5 files patched with CSS and prop fixes, no new files created.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api-routes-dashboard-core/03-UAT.md
@.planning/phases/03-api-routes-dashboard-core/03-05-SUMMARY.md
@.planning/phases/03-api-routes-dashboard-core/03-06-SUMMARY.md
@src/app/dashboard/page.tsx
@src/app/dashboard/architecture/page.tsx
@src/components/graph/architecture-graph.tsx
@src/components/graph/utils/layout.ts
@src/components/layout/dashboard-layout.tsx
@src/components/dashboard/agent-feed.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix overview graph height, architecture graph spacing, and agent feed full-height</name>
  <files>
    src/components/graph/utils/layout.ts
    src/components/graph/architecture-graph.tsx
    src/app/dashboard/page.tsx
    src/components/layout/dashboard-layout.tsx
    src/components/dashboard/agent-feed.tsx
  </files>
  <action>
    **Gap 1: Overview graph cramped (UAT test 5)**

    In `src/components/graph/architecture-graph.tsx`:
    - Add a `compact` optional boolean prop to the component: `interface ArchitectureGraphProps { compact?: boolean }`
    - When `compact` is true, do NOT render `<Controls>` or `<MiniMap>` — these waste space in the small overview card
    - Default `compact` to `false` so the Architecture tab page still gets Controls and MiniMap

    In `src/app/dashboard/page.tsx`:
    - Change the graph container from `h-[300px]` to `h-[500px]` — gives the graph enough vertical space to be navigable
    - Pass `compact` prop to the ArchitectureGraph: `<ArchitectureGraph compact />`
    - Remove `overflow-hidden` from the Card wrapping the graph (currently on line 70) — it clips the graph content. The ReactFlow container handles its own overflow

    **Gap 2: Architecture tab graph clustered (UAT test 6)**

    In `src/components/graph/utils/layout.ts`:
    - Change dagre `ranksep` from `80` to `180` — more horizontal spacing between rank columns
    - Change dagre `nodesep` from `40` to `80` — more vertical spacing between nodes in the same rank
    - Increase `NODE_WIDTH` from `180` to `200` and `NODE_HEIGHT` from `72` to `80` — match closer to actual rendered node size so dagre allocates correct space

    In `src/components/graph/architecture-graph.tsx`:
    - Change `fitViewOptions` padding from `0.2` to `0.4` — more breathing room around the graph edges
    - Add `maxZoom={1.2}` to the `<ReactFlow>` component — prevents over-zooming that makes nodes appear too large and clustered

    **Gap 3: Agent Activity panel half-height (UAT test 9)**

    In `src/components/layout/dashboard-layout.tsx`:
    - Add `h-full` to the right column div (line 31). Change from:
      `<div className="w-80 border-l border-slate-800 flex-shrink-0 overflow-hidden">`
      to:
      `<div className="w-80 border-l border-slate-800 flex-shrink-0 overflow-hidden h-full">`

    In `src/components/dashboard/agent-feed.tsx`:
    - Add `h-full` to the outer Card (line 65). Change from:
      `<Card className="bg-slate-800 border-slate-700 flex flex-col">`
      to:
      `<Card className="bg-slate-800 border-slate-700 flex flex-col h-full">`
    - Change the ScrollArea from hardcoded `h-[400px]` to `h-full` (line 89). The flex-1 on CardContent combined with h-full on ScrollArea will fill remaining space
    - Also add `min-h-0` to CardContent to enable flex shrinking in the column layout:
      `<CardContent className="flex-1 p-0 min-h-0">`
  </action>
  <verify>
    Run `pnpm typecheck` from project root — must pass with 0 errors (verifies compact prop types are correct).
    Run `pnpm build` from project root — must succeed (verifies no broken imports or JSX).
    Run `pnpm dev` and visually confirm:
    1. Overview page graph card is taller (~500px), no MiniMap/Controls in the overview card
    2. Architecture tab shows nodes spread out with generous spacing, not clustered
    3. Agent Activity panel extends full page height in right column
  </verify>
  <done>
    All 3 UAT gaps resolved: overview graph is 500px tall with compact mode (no MiniMap/Controls), architecture tab graph has ranksep:180/nodesep:80 with maxZoom:1.2, and agent feed panel fills full page height via h-full chain from layout to Card to ScrollArea.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fixed 3 UAT visual gaps: (1) Overview graph enlarged to 500px with compact mode hiding MiniMap/Controls, (2) Architecture tab graph spacing doubled (ranksep 80->180, nodesep 40->80) with maxZoom cap, (3) Agent Activity panel extended to full page height.</what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open http://localhost:3000
    2. Select "Trading Platform" from the architecture dropdown
    3. Navigate to Dashboard — **Overview page**: Verify the architecture graph card is noticeably taller, nodes are visible and not clipped, and there is NO MiniMap or Controls overlay in the overview card
    4. Click **Architecture** in the sidebar: Verify nodes are well-distributed across the viewport with generous spacing between them, not clustered in the center. The graph should use most of the available space. MiniMap and Controls should be visible on THIS page (not compact mode)
    5. Look at the **right column Agent Activity panel**: Verify it extends the full height of the page (from header to bottom), not cut off at half-height
  </how-to-verify>
  <resume-signal>Type "approved" if all 3 gaps are resolved, or describe any remaining issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes — compact prop correctly typed
- `pnpm build` succeeds — no broken JSX or imports
- Visual: Overview graph card is ~500px tall, no MiniMap/Controls in compact mode
- Visual: Architecture tab nodes distributed with ~2x more spacing than before
- Visual: Agent Activity panel fills full right-column height
</verification>

<success_criteria>
All 3 UAT gaps from 03-UAT.md are resolved:
1. Overview graph is navigable in its card (h-[500px], compact mode hides MiniMap/Controls)
2. Architecture tab graph looks clean with well-distributed nodes (ranksep:180, nodesep:80, padding:0.4, maxZoom:1.2)
3. Agent Activity panel extends full page height (h-full chain through layout, Card, ScrollArea)
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-routes-dashboard-core/03-07-SUMMARY.md`
</output>
