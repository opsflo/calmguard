---
phase: 05-testing-devsecops-dogfooding
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - vitest.config.mts
  - package.json
  - src/__tests__/calm/parser.test.ts
  - src/__tests__/calm/extractor.test.ts
  - src/__tests__/agents/schemas.test.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "pnpm test:run executes and all tests pass in under 30 seconds"
    - "CALM parser tests cover valid parsing, invalid input rejection, all 9 node types, and JSON string parsing"
    - "Zod schema validation tests verify agent event schema accepts valid events and rejects invalid shapes"
    - "Verbose test output shows individual test names for demo visibility"
  artifacts:
    - path: "vitest.config.mts"
      provides: "Vitest configuration with jsdom, vite-tsconfig-paths, verbose reporter"
      contains: "tsconfigPaths"
    - path: "src/__tests__/calm/parser.test.ts"
      provides: "CALM parser unit tests"
      min_lines: 50
    - path: "src/__tests__/calm/extractor.test.ts"
      provides: "CALM extractor unit tests"
      min_lines: 30
    - path: "src/__tests__/agents/schemas.test.ts"
      provides: "Agent Zod schema validation tests"
      min_lines: 40
  key_links:
    - from: "vitest.config.mts"
      to: "tsconfig.json"
      via: "vite-tsconfig-paths plugin resolves @/* aliases"
      pattern: "tsconfigPaths"
    - from: "src/__tests__/calm/parser.test.ts"
      to: "src/lib/calm/parser.ts"
      via: "imports parseCalm, parseCalmFromString"
      pattern: "import.*parseCalm"
    - from: "src/__tests__/agents/schemas.test.ts"
      to: "src/lib/agents/types.ts"
      via: "imports agentEventSchema for safeParse validation"
      pattern: "import.*agentEventSchema"
---

<objective>
Set up Vitest test framework and write the first ~10 unit tests covering CALM parsing logic (TEST-01) and agent output Zod schema validation (TEST-02).

Purpose: Establish the test infrastructure and cover the highest-value pure-function tests. These tests are a demo moment — judges watch ~10 tests fly by with clear names in under 10 seconds.

Output: Working vitest config, test scripts in package.json, ~10 passing tests for CALM parser + extractor + Zod schemas.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-testing-devsecops-dogfooding/05-RESEARCH.md

@src/lib/calm/parser.ts
@src/lib/calm/types.ts
@src/lib/calm/extractor.ts
@src/lib/agents/types.ts
@examples/trading-platform.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and configure test infrastructure</name>
  <files>vitest.config.mts, package.json</files>
  <action>
Install test dependencies:
```bash
pnpm add -D vitest @vitejs/plugin-react jsdom vite-tsconfig-paths
```

Create `vitest.config.mts` at project root with:
- `vite-tsconfig-paths` plugin FIRST (resolves `@/*` aliases — without this all imports break)
- `@vitejs/plugin-react` plugin second
- Environment: `jsdom`
- Globals: `true`
- Reporters: `[['verbose', { summary: false }]]` — per locked decision: verbose output with test names visible
- Pool: `forks` with `singleFork: false` for parallelism
- Include pattern: `src/__tests__/**/*.test.ts`

Add scripts to `package.json`:
- `"test"`: `"vitest"` (watch mode for dev)
- `"test:run"`: `"vitest run"` (single run for CI and demo)

Verify: `pnpm test:run` exits 0 (no tests found yet, but config works).
  </action>
  <verify>Run `pnpm test:run` — should exit cleanly with "no test files found" or similar. Run `pnpm exec vitest --version` to confirm installation.</verify>
  <done>vitest.config.mts exists with correct plugins, package.json has test and test:run scripts, vitest runs without config errors.</done>
</task>

<task type="auto">
  <name>Task 2: Write CALM parser, extractor, and Zod schema validation tests</name>
  <files>src/__tests__/calm/parser.test.ts, src/__tests__/calm/extractor.test.ts, src/__tests__/agents/schemas.test.ts</files>
  <action>
Create `src/__tests__/calm/parser.test.ts` with ~5 tests:
1. `parseCalm` — parses valid minimal CALM document (1 node, 0 relationships) → success true, 1 node
2. `parseCalm` — rejects document with empty nodes array → success false
3. `parseCalm` — rejects invalid node-type → success false
4. `parseCalm` — parses all 9 valid node types (actor, ecosystem, system, service, database, network, ldap, webclient, data-asset) → all success true
5. `parseCalmFromString` — parses valid JSON string → success true
6. `parseCalmFromString` — returns error for malformed JSON → success false, error message contains "JSON"

Use fixtures based on the CALM schema: minimal valid node has `unique-id`, `node-type`, `name`, `description`.

Create `src/__tests__/calm/extractor.test.ts` with ~2 tests:
1. `extractAnalysisInput` — extracts nodes and relationships from valid parsed CALM document
2. `extractAnalysisInput` — handles CALM document with relationships (connects type with source/destination)

Import from `@/lib/calm/extractor`. Use the trading-platform example JSON as reference for fixture shapes but create minimal inline fixtures.

Create `src/__tests__/agents/schemas.test.ts` with ~3 tests:
1. `agentEventSchema` — accepts valid "completed" event with all required fields (type, agent identity with name/displayName/icon/color, timestamp)
2. `agentEventSchema` — rejects event with invalid type string
3. `calmDocumentSchema` (from `@/lib/calm/types.ts`) — accepts valid CALM document structure

Read the actual Zod schemas in `src/lib/agents/types.ts` and `src/lib/calm/types.ts` to understand exact field names and shapes before writing tests. Do NOT guess schema shapes — read the source.

All tests use `import { describe, it, expect } from 'vitest'` (globals mode, but explicit imports for clarity).
  </action>
  <verify>Run `pnpm test:run` — all ~10 tests pass. Output shows verbose test names. Total runtime under 15 seconds.</verify>
  <done>~10 tests pass covering CALM parser (valid/invalid input, all node types, string parsing), extractor (node/relationship extraction), and Zod schemas (valid/invalid agent events, CALM document validation). Verbose output shows each test name.</done>
</task>

</tasks>

<verification>
1. `pnpm test:run` passes all tests with exit code 0
2. Verbose output shows individual test names (not just dots)
3. Total test runtime under 15 seconds
4. No `any` types in test files
5. Tests import from `@/lib/...` paths (proves vite-tsconfig-paths works)
</verification>

<success_criteria>
- Vitest configured with path alias resolution and verbose output
- ~10 tests covering CALM parsing + Zod schema validation
- All tests pass in under 15 seconds
- Test infrastructure ready for Plan 02 to add more tests
</success_criteria>

<output>
After completion, create `.planning/phases/05-testing-devsecops-dogfooding/05-01-SUMMARY.md`
</output>
