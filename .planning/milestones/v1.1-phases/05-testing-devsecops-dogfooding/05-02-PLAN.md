---
phase: 05-testing-devsecops-dogfooding
plan: 02
type: tdd
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/__tests__/api/analyze.test.ts
  - src/__tests__/api/parse.test.ts
  - src/__tests__/agents/orchestrator.test.ts
autonomous: true
requirements:
  - TEST-03
  - TEST-04
  - TEST-05

must_haves:
  truths:
    - "API route tests verify POST /api/calm/parse returns correct status codes and response shapes"
    - "API route tests verify POST /api/analyze returns SSE stream with correct content-type"
    - "Orchestrator test verifies parallel Phase 1 + sequential Phase 2 execution flow with mocked agents"
    - "All tests pass together with Plan 01 tests in under 30 seconds total"
  artifacts:
    - path: "src/__tests__/api/parse.test.ts"
      provides: "CALM parse API route contract tests"
      min_lines: 30
    - path: "src/__tests__/api/analyze.test.ts"
      provides: "Analyze API route SSE streaming tests"
      min_lines: 40
    - path: "src/__tests__/agents/orchestrator.test.ts"
      provides: "Orchestrator flow tests with mocked agents"
      min_lines: 50
  key_links:
    - from: "src/__tests__/api/parse.test.ts"
      to: "src/app/api/calm/parse/route.ts"
      via: "imports POST handler, calls with NextRequest"
      pattern: "import.*POST.*from.*api/calm/parse"
    - from: "src/__tests__/api/analyze.test.ts"
      to: "src/app/api/analyze/route.ts"
      via: "imports POST handler, reads SSE stream"
      pattern: "import.*POST.*from.*api/analyze"
    - from: "src/__tests__/agents/orchestrator.test.ts"
      to: "src/lib/agents/orchestrator.ts"
      via: "imports runAnalysis, mocks all 4 agents"
      pattern: "import.*runAnalysis"
---

<objective>
Write API route integration tests (TEST-03), SSE streaming tests (TEST-04), and mark TEST-05 as deferred. Adds ~5 more tests bringing total to ~15.

Purpose: Verify the API contract layer — request/response shapes match Zod schemas, SSE stream delivers correct content-type, orchestrator runs agents in correct parallel/sequential order. These are the second demo moment: "Our API contracts are tested."

Output: ~5 additional passing tests for API routes and orchestrator. Total suite: ~15 tests, under 30 seconds.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-testing-devsecops-dogfooding/05-RESEARCH.md
@.planning/phases/05-testing-devsecops-dogfooding/05-01-SUMMARY.md

@src/app/api/calm/parse/route.ts
@src/app/api/analyze/route.ts
@src/lib/agents/orchestrator.ts
@src/lib/agents/types.ts
@src/lib/ai/streaming.ts
@src/lib/ai/provider.ts
@src/lib/agents/registry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write API route contract tests and orchestrator flow test</name>
  <files>src/__tests__/api/parse.test.ts, src/__tests__/api/analyze.test.ts, src/__tests__/agents/orchestrator.test.ts</files>
  <action>
**IMPORTANT: Read all source files first** — `src/app/api/calm/parse/route.ts`, `src/app/api/analyze/route.ts`, `src/lib/agents/orchestrator.ts`, `src/lib/ai/provider.ts`, `src/lib/agents/registry.ts`, `src/lib/ai/streaming.ts` — to understand exact function signatures, imports, and module structure before writing any test.

Create `src/__tests__/api/parse.test.ts` with ~2 tests:
1. POST with valid CALM JSON → returns 200, body has expected shape (read the route to see what it returns)
2. POST with invalid CALM JSON (missing required fields) → returns 400 with error message

Tests construct `NextRequest` directly and call the exported `POST` handler. No HTTP server needed.
```typescript
import { NextRequest } from 'next/server';
import { POST } from '@/app/api/calm/parse/route';
```

Create `src/__tests__/api/analyze.test.ts` with ~2 tests:
1. POST with valid CALM JSON → response has Content-Type containing `text/event-stream`
2. POST with valid CALM JSON → response body is a ReadableStream that can be read

Mock the AI provider module and all agent functions to avoid real LLM calls:
```typescript
vi.mock('ai', async () => {
  const actual = await vi.importActual<typeof import('ai')>('ai');
  return { ...actual, generateObject: vi.fn() };
});
vi.mock('@/lib/ai/provider', () => ({
  getDefaultModel: vi.fn(() => 'mock-model'),
  getModelForAgent: vi.fn(() => 'mock-model'),
}));
```

Also mock `@/lib/agents/registry` (loadAgentConfig, loadSkills) to return fixture data so agents don't try to read YAML files.

Mock `generateObject` to return fixture data matching each agent's output schema shape. Read each agent file to understand what shape `generateObject` is expected to return.

Create `src/__tests__/agents/orchestrator.test.ts` with ~1 test:
1. `runAnalysis` calls agents in correct order — Phase 1 parallel (architecture, compliance, pipeline), then Phase 2 sequential (risk scorer). Mock all 4 agent modules to return fixture results. Verify risk scorer receives Phase 1 results.

Mock pattern for orchestrator test: Mock each agent module (`@/lib/agents/architecture-analyzer`, `@/lib/agents/compliance-mapper`, `@/lib/agents/pipeline-generator`, `@/lib/agents/risk-scorer`) to return predetermined fixture results. Also mock `@/lib/ai/streaming` event emitter if needed.

Per locked decision: TEST-05 (dashboard component tests) is deferred to post-hackathon. Do NOT write any React component tests. Add a comment in the test directory: `// TEST-05: Dashboard component tests deferred to post-hackathon (async server components not testable in jsdom)`

Per locked decision: Mock all LLM calls — no real API keys needed. Tests verify orchestration flow, not LLM quality.
  </action>
  <verify>Run `pnpm test:run` — ALL tests pass (Plan 01 + Plan 02 tests together). Total count ~15 tests. Total runtime under 30 seconds. Verbose output shows all test names.</verify>
  <done>~15 total tests pass in under 30 seconds. API routes verified for correct status codes and content-types. Orchestrator flow verified for parallel/sequential execution order. No real LLM calls made. TEST-05 documented as deferred.</done>
</task>

</tasks>

<verification>
1. `pnpm test:run` passes ALL tests (01 + 02) with exit code 0
2. Total test count is ~15
3. Total runtime under 30 seconds
4. No API keys required — all LLM calls mocked
5. API route tests use `NextRequest` directly (no HTTP server)
6. Verbose output lists every test name
</verification>

<success_criteria>
- ~15 total tests across 5 test files
- All pass in under 30 seconds
- API contracts verified (status codes, content-types, response shapes)
- Orchestrator flow verified (parallel Phase 1, sequential Phase 2)
- TEST-05 explicitly documented as deferred
</success_criteria>

<output>
After completion, create `.planning/phases/05-testing-devsecops-dogfooding/05-02-SUMMARY.md`
</output>
