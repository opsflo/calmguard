---
phase: 10.1-finos-contribution-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/add-license-headers.sh
  - src/**/*.ts
  - src/**/*.tsx
  - agents/*.yaml
  - skills/*.md
autonomous: true
requirements:
  - FINOS-01
must_haves:
  truths:
    - "Every .ts and .tsx file in src/ begins with '// SPDX-License-Identifier: Apache-2.0'"
    - "Every .yaml file in agents/ begins with '# SPDX-License-Identifier: Apache-2.0'"
    - "Every .md file in skills/ begins with the HTML comment SPDX identifier"
    - "Running the script a second time adds no duplicate headers (idempotent)"
    - "No config files (package.json, tsconfig.json, .env, next.config.ts) are modified"
  artifacts:
    - path: "scripts/add-license-headers.sh"
      provides: "Idempotent SPDX header prepend script"
      contains: "SPDX-License-Identifier: Apache-2.0"
  key_links:
    - from: "scripts/add-license-headers.sh"
      to: "src/**/*.{ts,tsx}"
      via: "find + shell loop with grep guard"
      pattern: "grep -q \"SPDX-License-Identifier\""
---

<objective>
Add SPDX Apache 2.0 license headers to all 127 source files (55 .ts, 57 .tsx in src/, 7 .yaml in agents/, 8 .md in skills/) using an idempotent bash script.

Purpose: FINOS maintainability audit requires SPDX identifiers. This is a mechanical fix with zero code-logic risk.
Output: scripts/add-license-headers.sh, all 127 source files with license headers prepended.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10.1-finos-contribution-readiness/10.1-CONTEXT.md
@.planning/phases/10.1-finos-contribution-readiness/10.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create add-license-headers.sh script</name>
  <files>scripts/add-license-headers.sh</files>
  <action>
Create /Users/gshah/work/opsflow-sh/hackathon/dtcch-2026-opsflow-llc/scripts/add-license-headers.sh with the following exact content:

```bash
#!/usr/bin/env bash
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 FINOS
#
# Add SPDX Apache 2.0 license headers to all CALMGuard source files.
# Idempotent — skips files that already have the SPDX identifier.
# Run from the repo root: bash scripts/add-license-headers.sh

set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$REPO_ROOT"

TS_HEADER='// SPDX-License-Identifier: Apache-2.0
// Copyright 2026 FINOS'

YAML_HEADER='# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 FINOS'

MD_HEADER='<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Copyright 2026 FINOS -->'

ADDED=0
SKIPPED=0

add_header() {
  local file="$1"
  local header="$2"

  # Idempotency guard: skip if SPDX already present
  if grep -q "SPDX-License-Identifier" "$file"; then
    SKIPPED=$((SKIPPED + 1))
    return
  fi

  local tmp
  tmp=$(mktemp)
  printf '%s\n\n' "$header" | cat - "$file" > "$tmp" && mv "$tmp" "$file"
  ADDED=$((ADDED + 1))
  echo "  + $file"
}

echo "Adding SPDX headers..."
echo ""

# TypeScript source files (src/**/*.ts and src/**/*.tsx)
echo "TypeScript / TSX files:"
while IFS= read -r -d '' file; do
  add_header "$file" "$TS_HEADER"
done < <(find src -type f \( -name "*.ts" -o -name "*.tsx" \) -print0)

# YAML agent definition files
echo ""
echo "YAML files (agents/):"
while IFS= read -r -d '' file; do
  add_header "$file" "$YAML_HEADER"
done < <(find agents -type f -name "*.yaml" -print0)

# Markdown skill files
echo ""
echo "Markdown files (skills/):"
while IFS= read -r -d '' file; do
  add_header "$file" "$MD_HEADER"
done < <(find skills -type f -name "*.md" -print0)

echo ""
echo "Done. Added: $ADDED, Skipped (already had header): $SKIPPED"
```

Make the script executable: `chmod +x scripts/add-license-headers.sh`
  </action>
  <verify>
    <automated>test -x /Users/gshah/work/opsflow-sh/hackathon/dtcch-2026-opsflow-llc/scripts/add-license-headers.sh && echo "script exists and is executable"</automated>
  </verify>
  <done>scripts/add-license-headers.sh exists, is executable, and contains the SPDX guard logic.</done>
</task>

<task type="auto">
  <name>Task 2: Run the script and verify idempotency</name>
  <files>src/**/*.ts, src/**/*.tsx, agents/*.yaml, skills/*.md</files>
  <action>
Run from the repo root:

```bash
cd /Users/gshah/work/opsflow-sh/hackathon/dtcch-2026-opsflow-llc
bash scripts/add-license-headers.sh
```

Observe the output — it should show "Added: ~127, Skipped: 0" on first run.

Run the script a second time to verify idempotency — it should show "Added: 0, Skipped: ~127".

Then spot-check three files to confirm the header format is correct:

1. A TypeScript file: head -3 src/lib/agents/orchestrator.ts
   Expected: `// SPDX-License-Identifier: Apache-2.0` on line 1

2. A YAML file: head -3 agents/orchestrator.yaml
   Expected: `# SPDX-License-Identifier: Apache-2.0` on line 1

3. A Markdown file: head -3 skills/SOX.md (or whichever skill file exists)
   Expected: `<!-- SPDX-License-Identifier: Apache-2.0 -->` on line 1

Verify no config files were touched:
```bash
head -1 package.json  # should still be '{'
head -1 tsconfig.json  # should still be '{'
```

After running, verify TypeScript still compiles (headers must not break imports):
```bash
cd /Users/gshah/work/opsflow-sh/hackathon/dtcch-2026-opsflow-llc && pnpm typecheck
```

Note: If typecheck finds issues unrelated to the headers (pre-existing), document them but do not block. The headers themselves cannot cause type errors.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/opsflow-sh/hackathon/dtcch-2026-opsflow-llc && head -1 src/lib/agents/orchestrator.ts | grep -q "SPDX-License-Identifier: Apache-2.0" && head -1 agents/orchestrator.yaml | grep -q "SPDX-License-Identifier: Apache-2.0" && echo "SPDX headers confirmed"</automated>
  </verify>
  <done>
All 127 source files have SPDX Apache 2.0 headers. Script is idempotent (second run adds 0 files). TypeScript compilation unaffected.
  </done>
</task>

</tasks>

<verification>
- grep -r "SPDX-License-Identifier" src/ | wc -l should equal the number of .ts + .tsx files
- grep -r "SPDX-License-Identifier" agents/ | wc -l should equal the number of .yaml files
- grep -r "SPDX-License-Identifier" skills/ | wc -l should equal the number of .md files
- head -1 package.json should NOT contain SPDX
- pnpm typecheck should exit 0 (or with same errors as before this plan ran)
</verification>

<success_criteria>
All source files (.ts, .tsx, .yaml, .md in scope directories) contain SPDX Apache 2.0 identifier on line 1. Script is idempotent. No config files modified. TypeScript compilation unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-finos-contribution-readiness/10.1-01-SUMMARY.md`
</output>
