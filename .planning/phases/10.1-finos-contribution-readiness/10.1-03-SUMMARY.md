---
phase: 10.1-finos-contribution-readiness
plan: "03"
subsystem: contributing-docs-and-component-tests
tags: [contributing, testing, snapshot-tests, react-testing-library, vitest, finos]
dependency_graph:
  requires: []
  provides:
    - CONTRIBUTING.md "Extending CALMGuard" section
    - vitest tsx test discovery
    - compliance-card snapshot test
    - findings-table snapshot test
    - risk-heat-map snapshot test
  affects:
    - CI test pipeline (16 suites now, 3 new component tests)
    - FINOS contributor documentation (agent, skill, framework extension guide)
tech_stack:
  added:
    - "@testing-library/react 16.3.2"
    - "@testing-library/jest-dom 6.9.1"
  patterns:
    - "vi.mock('@/store/analysis-store') for Zustand isolation in component tests"
    - "Dynamic import after vi.mock to ensure mock is applied before component loads"
    - "global.requestAnimationFrame mock for jsdom rAF compatibility"
    - "toMatchSnapshot() for stable UI regression baseline"
key_files:
  created:
    - src/__tests__/components/compliance-card.test.tsx
    - src/__tests__/components/findings-table.test.tsx
    - src/__tests__/components/risk-heat-map.test.tsx
    - src/__tests__/components/__snapshots__/compliance-card.test.tsx.snap
    - src/__tests__/components/__snapshots__/findings-table.test.tsx.snap
    - src/__tests__/components/__snapshots__/risk-heat-map.test.tsx.snap
  modified:
    - CONTRIBUTING.md
    - vitest.config.mts
    - package.json
    - pnpm-lock.yaml
decisions:
  - "@testing-library/react dynamic import after vi.mock to guarantee Zustand mock is registered before component module loads"
  - "requestAnimationFrame mocked globally via beforeAll() for ComplianceCard useCountUp hook compatibility with jsdom"
  - "Zustand mock uses selector pattern (vi.fn((selector) => selector({...}))) to match how useAnalysisStore is called in components"
metrics:
  duration: "8min"
  completed: "2026-03-06"
  tasks_completed: 2
  tasks_total: 2
  files_modified: 8
---

# Phase 10.1 Plan 03: Contributing Docs and React Component Tests Summary

CONTRIBUTING.md extended with Extending CALMGuard section (agent/skill/framework authoring guide), and React component snapshot tests added for 3 dashboard components using @testing-library/react with Zustand store mocks.

## Tasks Completed

| # | Task | Commit | Key Files |
|---|------|--------|-----------|
| 1 | Add "Extending CALMGuard" section to CONTRIBUTING.md | 7b1314e | CONTRIBUTING.md |
| 2 | Install @testing-library/react and add React component tests | 0848a0b | vitest.config.mts, 3x .test.tsx, 3x .snap |

## What Was Built

### Task 1: CONTRIBUTING.md "Extending CALMGuard" Section

Added 61 lines at end of CONTRIBUTING.md (after Security section) covering:
- **Adding a New Agent**: 5-step walkthrough — YAML definition, TypeScript implementation with Zod schema + `generateObject`, registry registration, orchestrator wiring, and test requirements
- **Adding a New Compliance Skill**: 4-step guide — Markdown skill file format, YAML reference, Zod enum extension, verification steps
- **Adding a New Compliance Framework (UI)**: 3-step shortcut for UI-only framework additions
- **Project Conventions**: Reminder of no-any rule, generateObject requirement, SPDX headers, DCO sign-off

### Task 2: React Component Snapshot Tests

Updated `vitest.config.mts` include pattern from `*.test.ts` to `*.test.{ts,tsx}` to enable tsx test file discovery.

Created 3 component tests in `src/__tests__/components/`:

**compliance-card.test.tsx:**
- Mocks `requestAnimationFrame` and `cancelAnimationFrame` globally (jsdom compatibility for `useCountUp` hook)
- Mocks Zustand `useAnalysisStore` with `{ analysisResult: null, status: 'idle' }` state
- Uses dynamic import after mocks to ensure mock registration order
- Snapshot captures the idle/empty state (dashed gauge ring, placeholder framework bars)

**findings-table.test.tsx:**
- Mocks Zustand store with idle state
- Renders the "Run analysis to view findings" empty state card
- Snapshot baseline established

**risk-heat-map.test.tsx:**
- Mocks Zustand store with `{ analysisResult: null, status: 'idle' }`
- Renders the "Run analysis to view risk heat map" empty state card
- Snapshot baseline established

## Verification Results

```
Test Files  16 passed (16)
      Tests  114 passed (114)
   Snapshots  3 written
```

All 13 existing suites still pass. 3 new component test suites pass. Exit 0.

```
grep -c "Extending CALMGuard" CONTRIBUTING.md
→ 1

grep "tsx" vitest.config.mts
→ include: ['src/__tests__/**/*.test.{ts,tsx}'],
```

## Deviations from Plan

None — plan executed exactly as written. The dynamic import pattern and Zustand selector mock pattern from the plan's interface section worked without modifications.

## Self-Check

- [x] CONTRIBUTING.md has "Extending CALMGuard" section: FOUND at line 149
- [x] vitest.config.mts includes .tsx pattern: CONFIRMED
- [x] compliance-card.test.tsx exists with passing snapshot: CONFIRMED
- [x] findings-table.test.tsx exists with passing snapshot: CONFIRMED
- [x] risk-heat-map.test.tsx exists with passing snapshot: CONFIRMED
- [x] Commits 7b1314e and 0848a0b exist: CONFIRMED via git log

## Self-Check: PASSED
