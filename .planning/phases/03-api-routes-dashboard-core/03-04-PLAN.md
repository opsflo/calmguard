---
phase: 03-api-routes-dashboard-core
plan: 04
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - src/components/dashboard/agent-feed.tsx
  - src/components/dashboard/agent-feed-event.tsx
  - src/app/globals.css
autonomous: true
requirements: [FEED-01, FEED-02, FEED-03, FEED-04, FEED-05]

must_haves:
  truths:
    - "Agent feed shows real-time scrolling list of events from Zustand store"
    - "Each event displays agent icon, agent name, timestamp, and message"
    - "Events slide in from right with animation"
    - "Finding events show severity badge with color coding"
    - "Feed auto-scrolls to latest event as new events arrive"
    - "Thinking events show animated pulsing dots"
  artifacts:
    - path: "src/components/dashboard/agent-feed.tsx"
      provides: "Agent feed container with ScrollArea and auto-scroll"
      exports: ["AgentFeed"]
    - path: "src/components/dashboard/agent-feed-event.tsx"
      provides: "Individual event row with animation and severity badge"
      exports: ["AgentFeedEvent"]
  key_links:
    - from: "src/components/dashboard/agent-feed.tsx"
      to: "src/store/analysis-store.ts"
      via: "useAnalysisStore selector for agentEvents"
      pattern: "useAnalysisStore.*agentEvents"
    - from: "src/components/dashboard/agent-feed-event.tsx"
      to: "src/lib/agents/types.ts"
      via: "AgentEvent type for event structure"
      pattern: "AgentEvent"
---

<objective>
Build the real-time agent activity feed component that displays streaming events as agents work. The feed is a scrolling log-like panel showing each agent event with identity, timestamp, message, severity badges, and slide-in animations.

Purpose: The agent feed is the primary real-time feedback mechanism — it shows users that AI agents are actively working and what they're finding. Per locked decision, it should feel like a live log pane with full visibility into agent reasoning.

Output: 2 new files (feed container + event row), 1 modified file (CSS animations)
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api-routes-dashboard-core/03-RESEARCH.md
@src/lib/agents/types.ts
@src/store/analysis-store.ts
@src/components/ui/badge.tsx
@src/components/ui/scroll-area.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AgentFeedEvent component with animations and severity badges</name>
  <files>src/components/dashboard/agent-feed-event.tsx, src/app/globals.css</files>
  <action>
    **src/components/dashboard/agent-feed-event.tsx** (NEW):
    Create the individual event row component. Mark as 'use client'.

    Props: `{ event: AgentEvent, index: number }` — index used for staggered animation delay.

    Layout per event (horizontal):
    - Left: Agent icon (use lucide-react dynamic icon lookup — the agent.icon field contains a lucide icon name like 'search', 'shield', 'git-branch', 'bar-chart'). Fall back to a generic Activity icon if dynamic lookup is complex. Keep it simple: use a mapped object `{ search: Search, shield: Shield, 'git-branch': GitBranch, 'bar-chart': BarChart3, layers: Layers }` for the 5 agent icons.
    - Agent icon colored with `agent.color` (use inline style `color: agent.color` or map to Tailwind classes)
    - Middle column:
      - Top row: Agent displayName (bold, slate-200) + timestamp (right-aligned, slate-500, format as HH:MM:SS)
      - Bottom row: Event message (slate-400)
    - Right: Severity badge (if event has severity field)

    Severity badge mapping:
    - `critical`: Badge with `bg-red-500/20 text-red-400 border-red-500/50`
    - `high`: Badge with `bg-orange-500/20 text-orange-400 border-orange-500/50`
    - `medium`: Badge with `bg-amber-500/20 text-amber-400 border-amber-500/50`
    - `low`: Badge with `bg-blue-500/20 text-blue-400 border-blue-500/50`
    - `info`: Badge with `bg-slate-500/20 text-slate-400 border-slate-500/50`

    Event type styling:
    - `started`: Normal message, possibly with a "play" indicator
    - `thinking`: Message with animated pulsing dots ("Analyzing architecture..."). Add `<span className="animate-pulse">...</span>` after the message text when type is 'thinking'
    - `finding`: Normal message with severity badge shown
    - `completed`: Message with checkmark indicator
    - `error`: Red-tinted background `bg-red-500/5`

    Animation: Apply CSS animation class for slide-in-from-right:
    - Each event gets `animate-slide-in-right` class
    - Use staggered delay based on index: `style={{ animationDelay: \`${Math.min(index * 50, 500)}ms\` }}`
    - Cap the delay so older events don't wait forever

    **src/app/globals.css** (MODIFY):
    Add custom keyframes for slide-in animation using Tailwind v4 @theme syntax:

    ```css
    @keyframes slide-in-right {
      from {
        transform: translateX(20px);
        opacity: 0;
      }
      to {
        transform: translateX(0);
        opacity: 1;
      }
    }
    ```

    Add to the @theme block (or create one if needed):
    ```css
    @theme {
      --animate-slide-in-right: slide-in-right 0.3s ease-out forwards;
    }
    ```

    Note: Check the existing globals.css for any existing @theme block and add to it rather than creating a duplicate. The animation fill-mode should be `forwards` so the element stays visible after animation.
  </action>
  <verify>Run `pnpm typecheck` — must pass. Verify AgentFeedEvent renders correctly for each event type (started, thinking, finding, completed, error).</verify>
  <done>AgentFeedEvent component renders each event with agent icon, timestamp, message, severity badge, and slide-in animation. Thinking events show pulsing dots.</done>
</task>

<task type="auto">
  <name>Task 2: Create AgentFeed container with ScrollArea and auto-scroll behavior</name>
  <files>src/components/dashboard/agent-feed.tsx</files>
  <action>
    **src/components/dashboard/agent-feed.tsx** (NEW):
    Create the feed container component. Mark as 'use client'.

    Replace the existing agent-feed-skeleton.tsx functionality with a real component.

    Structure:
    1. Read events from store: `const agentEvents = useAnalysisStore(state => state.agentEvents);`
    2. Read status from store: `const status = useAnalysisStore(state => state.status);`
    3. Maintain a ref to the bottom of the list: `const bottomRef = useRef<HTMLDivElement>(null);`
    4. Auto-scroll effect:
       ```typescript
       useEffect(() => {
         bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
       }, [agentEvents.length]);
       ```

    Render:
    - Card wrapper with `bg-slate-800 border-slate-700` styling
    - Card header: "Agent Activity" title with a small live indicator (green dot with pulse when status === 'analyzing')
    - Card body: ScrollArea (from shadcn) with fixed height (e.g., h-[400px] or flex-1 depending on context)
    - Inside ScrollArea: Map over `agentEvents` rendering `<AgentFeedEvent event={event} index={i} key={i} />`
    - At bottom of list: `<div ref={bottomRef} />`

    Empty state (when agentEvents.length === 0 and status is idle or parsed):
    - Centered text: "Agent events will appear here during analysis"
    - Muted styling: text-slate-500, italic

    Empty state (when agentEvents.length === 0 and status is analyzing):
    - "Waiting for agent events..." with pulsing animation

    Event count badge in header: Show `{agentEvents.length} events` badge when events exist.

    The component should be designed to work both in the right-column layout panel and as a standalone card in the overview grid.
  </action>
  <verify>Run `pnpm typecheck` and `pnpm build` — both must pass. Verify AgentFeed reads from store, renders events, and has auto-scroll behavior.</verify>
  <done>AgentFeed container renders scrolling event list with auto-scroll, empty states, and live indicator. Ready to be placed in dashboard layout right column.</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes with zero errors
2. `pnpm build` succeeds
3. AgentFeedEvent renders all 5 event types with appropriate styling
4. Severity badges render with correct colors (red, orange, amber, blue, slate)
5. Thinking events show animated pulsing dots
6. AgentFeed auto-scrolls to bottom on new events
7. Custom slide-in-right animation defined in globals.css
8. Empty states shown when no events exist
</verification>

<success_criteria>
- Feed shows every event type: started, thinking, finding, completed, error
- Each event shows agent icon + name, timestamp (HH:MM:SS), message
- Finding events show severity badge with color coding
- Feed auto-scrolls to latest event during analysis
- Slide-in animation on new events
- Thinking events show animated pulsing dots
- Empty state shown pre-analysis
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-routes-dashboard-core/03-04-SUMMARY.md`
</output>
