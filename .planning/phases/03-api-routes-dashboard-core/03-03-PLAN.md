---
phase: 03-api-routes-dashboard-core
plan: 03
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - src/components/layout/dashboard-layout.tsx
  - src/components/dashboard/sidebar.tsx
  - src/components/dashboard/header.tsx
  - src/components/dashboard/analyze-button.tsx
  - src/app/dashboard/page.tsx
autonomous: true
requirements: [DASH-01, DASH-03, DASH-04]

must_haves:
  truths:
    - "Dashboard has dark theme (slate-900/950) with left sidebar navigation"
    - "Sidebar shows agent status indicators that update in real-time from store"
    - "Header contains architecture selector dropdown to switch architectures"
    - "Header has explicit Analyze button that triggers analysis"
    - "Main content area uses flex layout with right column reserved for feed panel"
    - "User can navigate tabs (Overview, Architecture, Compliance, Pipeline, Findings) during analysis"
  artifacts:
    - path: "src/components/dashboard/sidebar.tsx"
      provides: "Sidebar with live agent status dots from Zustand store"
      contains: "getAgentStatus"
    - path: "src/components/dashboard/header.tsx"
      provides: "Header with architecture selector dropdown and Analyze button"
      contains: "useAgentStream"
    - path: "src/components/dashboard/analyze-button.tsx"
      provides: "Analyze button component with loading state"
      exports: ["AnalyzeButton"]
    - path: "src/components/layout/dashboard-layout.tsx"
      provides: "Dashboard layout with right column for feed panel"
      contains: "flex"
  key_links:
    - from: "src/components/dashboard/sidebar.tsx"
      to: "src/store/analysis-store.ts"
      via: "useAnalysisStore selector for agentEvents and activeAgents"
      pattern: "useAnalysisStore"
    - from: "src/components/dashboard/header.tsx"
      to: "src/hooks/use-agent-stream.ts"
      via: "useAgentStream for triggering analysis"
      pattern: "useAgentStream"
    - from: "src/components/dashboard/header.tsx"
      to: "src/store/analysis-store.ts"
      via: "store for rawCalmData and status"
      pattern: "useAnalysisStore"
---

<objective>
Upgrade the dashboard shell from Phase 1 skeleton to a real-time layout: Sidebar with live agent status dots, Header with architecture selector dropdown and Analyze button, and a layout structure with right-column space for the feed panel.

Purpose: Transforms the static Phase 1 dashboard skeleton into an interactive shell that drives the analysis workflow. User selects architecture in header, clicks Analyze, and watches agent dots light up in sequence in the sidebar.

Output: 1 new file (analyze-button), 4 modified files (layout, sidebar, header, dashboard page)
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api-routes-dashboard-core/03-RESEARCH.md
@.planning/phases/03-api-routes-dashboard-core/03-02-SUMMARY.md
@src/components/layout/dashboard-layout.tsx
@src/components/dashboard/sidebar.tsx
@src/components/dashboard/header.tsx
@src/app/dashboard/page.tsx
@src/store/analysis-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Sidebar with live agent status dots from Zustand store</name>
  <files>src/components/dashboard/sidebar.tsx</files>
  <action>
    Rewrite the Sidebar component to derive agent status from the Zustand store instead of using hardcoded static data.

    Changes:
    1. Import `useAnalysisStore`, `getAgentStatus`, `AGENT_NAMES`, `AGENT_DISPLAY_NAMES` from store
    2. Remove the hardcoded `agents: AgentStatus[]` array and `AgentStatus` interface
    3. In the component body, read from store:
       ```typescript
       const agentEvents = useAnalysisStore(state => state.agentEvents);
       const activeAgents = useAnalysisStore(state => state.activeAgents);
       ```
       Use individual selectors (not destructuring) for performance per research Zustand pattern.
    4. Replace the static agent list with a dynamic map over `AGENT_NAMES`:
       ```tsx
       {AGENT_NAMES.map((name) => {
         const status = getAgentStatus(agentEvents, activeAgents, name);
         return (
           <div key={name} className="flex items-center gap-2 px-3 py-1.5 text-sm">
             <div className={cn('h-2 w-2 rounded-full', statusColors[status])} />
             <span className="text-slate-400 text-xs">{AGENT_DISPLAY_NAMES[name]}</span>
           </div>
         );
       })}
       ```
    5. Keep the existing nav items, styling, logo, and footer unchanged
    6. Keep `statusColors` mapping: `{ idle: 'bg-slate-600', running: 'bg-blue-500 animate-pulse', complete: 'bg-emerald-500', error: 'bg-red-500' }`

    Claude's discretion: Sidebar stays fixed width (w-64), not collapsible. Simple for hackathon demo.
  </action>
  <verify>Run `pnpm typecheck` — must pass. Verify sidebar imports getAgentStatus from store.</verify>
  <done>Sidebar agent dots are reactive — they read from store and update as agentEvents arrive during analysis.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Header with architecture selector, Analyze button, and update dashboard layout</name>
  <files>src/components/dashboard/header.tsx, src/components/dashboard/analyze-button.tsx, src/components/layout/dashboard-layout.tsx, src/app/dashboard/page.tsx</files>
  <action>
    **src/components/dashboard/analyze-button.tsx** (NEW):
    Create a dedicated Analyze button component:
    - Props: `onAnalyze: () => void`, `disabled: boolean`, `loading: boolean`
    - Renders a Button with Play icon (from lucide-react)
    - When `loading`: show Loader2 icon with animate-spin, text "Analyzing..."
    - When `disabled`: grayed out
    - Normal state: emerald/green accent button "Analyze"
    - Use shadcn Button component with appropriate variant

    **src/components/dashboard/header.tsx** (MODIFY):
    Transform from simple status badge to full control bar.

    1. Import `useAnalysisStore` for: rawCalmData, selectedDemoId, status, setCalmData, setSelectedDemo
    2. Import `useAgentStream` hook
    3. Import demo architectures list for the dropdown (from the examples directory — check how Phase 1 landing page loads them)
    4. Import shadcn Select component
    5. Import AnalyzeButton component

    Layout (left to right):
    - Left: "Dashboard" breadcrumb text (keep existing)
    - Center/Right: Architecture selector dropdown (Select component with demo options), Analyze button, parse status badge

    Architecture selector:
    - Show current selection name or "Select Architecture..."
    - Options: Trading Platform, Payment Gateway (from demo list)
    - On change: Load the selected demo CALM data, parse it, store in Zustand (reuse the pattern from Phase 1 landing page)
    - Per locked decision: "Landing page for first-time selection, header dropdown to switch without leaving dashboard after initial pick"

    Analyze button:
    - Disabled when no `rawCalmData` in store (nothing to analyze)
    - Disabled when `status === 'analyzing'` (already running)
    - Loading state when `status === 'analyzing'`
    - On click: call `startStream(rawCalmData)` from useAgentStream hook

    Parse status badge: Keep existing badge showing "Parsed: N nodes, M relationships" when analysisInput exists

    **src/components/layout/dashboard-layout.tsx** (MODIFY):
    Update layout to reserve space for the agent feed in a right column:
    - Change the main content area from just `{children}` to a flex row:
      ```
      <main className="flex-1 flex overflow-hidden">
        <div className="flex-1 overflow-auto">
          {children}
        </div>
        {/* Right column for agent feed - rendered by dashboard pages */}
      </main>
      ```
    - Per locked decision: "Feed lives in a dedicated right column panel, always visible alongside main content"
    - The actual feed component will be placed here in Plan 03-05. For now, leave the right column slot ready by ensuring the layout uses flex-row for main content area.

    **src/app/dashboard/page.tsx** (MODIFY):
    Update the empty state message to match locked decision:
    - Pre-analysis: "Select an architecture and click Analyze" call-to-action
    - Remove the "Back to Home" link (user can now select architecture from header)
    - Keep the skeleton grid but update the empty state text
  </action>
  <verify>Run `pnpm typecheck` and `pnpm build` — both must pass. Verify Header renders a Select dropdown and AnalyzeButton.</verify>
  <done>Header has architecture selector dropdown and Analyze button. Layout reserves right column for feed. Dashboard empty state matches design spec.</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes with zero errors
2. `pnpm build` succeeds
3. Sidebar agent dots are driven by store (no hardcoded agents array)
4. Header contains architecture Select dropdown and Analyze button
5. Analyze button calls useAgentStream.startStream when clicked
6. Layout uses flex row to allow right-column feed placement
7. Dashboard empty state says "Select an architecture and click Analyze"
</verification>

<success_criteria>
- Sidebar agent status dots update reactively from Zustand store
- Header allows switching architectures without leaving dashboard
- Explicit "Analyze" button triggers analysis (not auto-start on selection)
- Layout structure accommodates always-visible right column for feed
- Navigation tabs remain clickable during analysis
- Dark theme maintained (slate-900 bg, slate-800 cards)
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-routes-dashboard-core/03-03-SUMMARY.md`
</output>
